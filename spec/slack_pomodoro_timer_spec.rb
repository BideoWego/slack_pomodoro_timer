require 'spec_helper'

describe SlackPomodoroTimer do
  it 'has a version number' do
    expect(SlackPomodoroTimer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
